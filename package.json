{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview --port 3001",
    "deploy": "sh deploy.sh"
  },
  "devDependencies": {
    "vite": "^2.4.4"
  }
}
